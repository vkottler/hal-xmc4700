/**
 * \file
 * \brief Generated by ifgen (3.1.0).
 */
#pragma once

#include "../enums/FCE_KE0_CFG_ALR.h"
#include "../enums/FCE_KE0_CFG_BEI.h"
#include "../enums/FCE_KE0_CFG_CCE.h"
#include "../enums/FCE_KE0_CFG_CEI.h"
#include "../enums/FCE_KE0_CFG_CMI.h"
#include "../enums/FCE_KE0_CFG_LEI.h"
#include "../enums/FCE_KE0_CFG_REFIN.h"
#include "../enums/FCE_KE0_CFG_REFOUT.h"
#include "../enums/FCE_KE0_CFG_XSEL.h"
#include "../ifgen/common.h"

namespace XMC4700
{

/**
 * Flexible CRC Engine
 */
struct [[gnu::packed]] fce_ke0
{
    /* Constant attributes. */
    static constexpr std::size_t size = 32; /*!< fce_ke0's size in bytes. */

    /* Fields. */
    uint32_t IR;             /*!< (read-write) Input Register */
    const uint32_t RES = {}; /*!< (read-only) CRC Result Register */
    uint32_t CFG;            /*!< (read-write) CRC Configuration Register */
    uint32_t STS;            /*!< (read-write) CRC Status Register */
    uint32_t LENGTH;         /*!< (read-write) CRC Length Register */
    uint32_t CHECK;          /*!< (read-write) CRC Check Register */
    uint32_t CRC;            /*!< (read-write) CRC Register */
    uint32_t CTR;            /*!< (read-write) CRC Test Register */

    /* Methods. */

    /**
     * Get IR's IR field.
     */
    inline uint32_t get_IR_IR()
    {
        return (IR >> 0u) & 0b11111111111111111111111111111111u;
    }

    /**
     * Set IR's IR field.
     */
    inline void set_IR_IR(uint32_t value)
    {
        uint32_t curr = IR;

        curr &= ~(0b11111111111111111111111111111111u << 0u);
        curr |= (value & 0b11111111111111111111111111111111u) << 0u;

        IR = curr;
    }

    /**
     * Get RES's RES field.
     */
    inline uint32_t get_RES_RES()
    {
        return (RES >> 0u) & 0b11111111111111111111111111111111u;
    }

    /**
     * Get CFG's XSEL bit.
     */
    inline FCE_KE0_CFG_XSEL get_CFG_XSEL()
    {
        return FCE_KE0_CFG_XSEL(CFG & (1u << 10u));
    }

    /**
     * Set CFG's XSEL bit.
     */
    inline void set_CFG_XSEL()
    {
        CFG |= 1u << 10u;
    }

    /**
     * Clear CFG's XSEL bit.
     */
    inline void clear_CFG_XSEL()
    {
        CFG &= ~(1u << 10u);
    }

    /**
     * Toggle CFG's XSEL bit.
     */
    inline void toggle_CFG_XSEL()
    {
        CFG ^= 1u << 10u;
    }

    /**
     * Get CFG's REFOUT bit.
     */
    inline FCE_KE0_CFG_REFOUT get_CFG_REFOUT()
    {
        return FCE_KE0_CFG_REFOUT(CFG & (1u << 9u));
    }

    /**
     * Set CFG's REFOUT bit.
     */
    inline void set_CFG_REFOUT()
    {
        CFG |= 1u << 9u;
    }

    /**
     * Clear CFG's REFOUT bit.
     */
    inline void clear_CFG_REFOUT()
    {
        CFG &= ~(1u << 9u);
    }

    /**
     * Toggle CFG's REFOUT bit.
     */
    inline void toggle_CFG_REFOUT()
    {
        CFG ^= 1u << 9u;
    }

    /**
     * Get CFG's REFIN bit.
     */
    inline FCE_KE0_CFG_REFIN get_CFG_REFIN()
    {
        return FCE_KE0_CFG_REFIN(CFG & (1u << 8u));
    }

    /**
     * Set CFG's REFIN bit.
     */
    inline void set_CFG_REFIN()
    {
        CFG |= 1u << 8u;
    }

    /**
     * Clear CFG's REFIN bit.
     */
    inline void clear_CFG_REFIN()
    {
        CFG &= ~(1u << 8u);
    }

    /**
     * Toggle CFG's REFIN bit.
     */
    inline void toggle_CFG_REFIN()
    {
        CFG ^= 1u << 8u;
    }

    /**
     * Get CFG's ALR bit.
     */
    inline FCE_KE0_CFG_ALR get_CFG_ALR()
    {
        return FCE_KE0_CFG_ALR(CFG & (1u << 5u));
    }

    /**
     * Set CFG's ALR bit.
     */
    inline void set_CFG_ALR()
    {
        CFG |= 1u << 5u;
    }

    /**
     * Clear CFG's ALR bit.
     */
    inline void clear_CFG_ALR()
    {
        CFG &= ~(1u << 5u);
    }

    /**
     * Toggle CFG's ALR bit.
     */
    inline void toggle_CFG_ALR()
    {
        CFG ^= 1u << 5u;
    }

    /**
     * Get CFG's CCE bit.
     */
    inline FCE_KE0_CFG_CCE get_CFG_CCE()
    {
        return FCE_KE0_CFG_CCE(CFG & (1u << 4u));
    }

    /**
     * Set CFG's CCE bit.
     */
    inline void set_CFG_CCE()
    {
        CFG |= 1u << 4u;
    }

    /**
     * Clear CFG's CCE bit.
     */
    inline void clear_CFG_CCE()
    {
        CFG &= ~(1u << 4u);
    }

    /**
     * Toggle CFG's CCE bit.
     */
    inline void toggle_CFG_CCE()
    {
        CFG ^= 1u << 4u;
    }

    /**
     * Get CFG's BEI bit.
     */
    inline FCE_KE0_CFG_BEI get_CFG_BEI()
    {
        return FCE_KE0_CFG_BEI(CFG & (1u << 3u));
    }

    /**
     * Set CFG's BEI bit.
     */
    inline void set_CFG_BEI()
    {
        CFG |= 1u << 3u;
    }

    /**
     * Clear CFG's BEI bit.
     */
    inline void clear_CFG_BEI()
    {
        CFG &= ~(1u << 3u);
    }

    /**
     * Toggle CFG's BEI bit.
     */
    inline void toggle_CFG_BEI()
    {
        CFG ^= 1u << 3u;
    }

    /**
     * Get CFG's LEI bit.
     */
    inline FCE_KE0_CFG_LEI get_CFG_LEI()
    {
        return FCE_KE0_CFG_LEI(CFG & (1u << 2u));
    }

    /**
     * Set CFG's LEI bit.
     */
    inline void set_CFG_LEI()
    {
        CFG |= 1u << 2u;
    }

    /**
     * Clear CFG's LEI bit.
     */
    inline void clear_CFG_LEI()
    {
        CFG &= ~(1u << 2u);
    }

    /**
     * Toggle CFG's LEI bit.
     */
    inline void toggle_CFG_LEI()
    {
        CFG ^= 1u << 2u;
    }

    /**
     * Get CFG's CEI bit.
     */
    inline FCE_KE0_CFG_CEI get_CFG_CEI()
    {
        return FCE_KE0_CFG_CEI(CFG & (1u << 1u));
    }

    /**
     * Set CFG's CEI bit.
     */
    inline void set_CFG_CEI()
    {
        CFG |= 1u << 1u;
    }

    /**
     * Clear CFG's CEI bit.
     */
    inline void clear_CFG_CEI()
    {
        CFG &= ~(1u << 1u);
    }

    /**
     * Toggle CFG's CEI bit.
     */
    inline void toggle_CFG_CEI()
    {
        CFG ^= 1u << 1u;
    }

    /**
     * Get CFG's CMI bit.
     */
    inline FCE_KE0_CFG_CMI get_CFG_CMI()
    {
        return FCE_KE0_CFG_CMI(CFG & (1u << 0u));
    }

    /**
     * Set CFG's CMI bit.
     */
    inline void set_CFG_CMI()
    {
        CFG |= 1u << 0u;
    }

    /**
     * Clear CFG's CMI bit.
     */
    inline void clear_CFG_CMI()
    {
        CFG &= ~(1u << 0u);
    }

    /**
     * Toggle CFG's CMI bit.
     */
    inline void toggle_CFG_CMI()
    {
        CFG ^= 1u << 0u;
    }

    /**
     * Get all of CFG's bit fields.
     */
    inline void get_CFG(FCE_KE0_CFG_XSEL &XSEL, FCE_KE0_CFG_REFOUT &REFOUT, FCE_KE0_CFG_REFIN &REFIN, FCE_KE0_CFG_ALR &ALR, FCE_KE0_CFG_CCE &CCE, FCE_KE0_CFG_BEI &BEI, FCE_KE0_CFG_LEI &LEI, FCE_KE0_CFG_CEI &CEI, FCE_KE0_CFG_CMI &CMI)
    {
        uint32_t curr = CFG;

        XSEL = FCE_KE0_CFG_XSEL(curr & (1u << 10u));
        REFOUT = FCE_KE0_CFG_REFOUT(curr & (1u << 9u));
        REFIN = FCE_KE0_CFG_REFIN(curr & (1u << 8u));
        ALR = FCE_KE0_CFG_ALR(curr & (1u << 5u));
        CCE = FCE_KE0_CFG_CCE(curr & (1u << 4u));
        BEI = FCE_KE0_CFG_BEI(curr & (1u << 3u));
        LEI = FCE_KE0_CFG_LEI(curr & (1u << 2u));
        CEI = FCE_KE0_CFG_CEI(curr & (1u << 1u));
        CMI = FCE_KE0_CFG_CMI(curr & (1u << 0u));
    }

    /**
     * Set all of CFG's bit fields.
     */
    inline void set_CFG(FCE_KE0_CFG_XSEL XSEL, FCE_KE0_CFG_REFOUT REFOUT, FCE_KE0_CFG_REFIN REFIN, FCE_KE0_CFG_ALR ALR, FCE_KE0_CFG_CCE CCE, FCE_KE0_CFG_BEI BEI, FCE_KE0_CFG_LEI LEI, FCE_KE0_CFG_CEI CEI, FCE_KE0_CFG_CMI CMI)
    {
        uint32_t curr = CFG;

        curr &= ~(0b1u << 10u);
        curr |= (std::to_underlying(XSEL) & 0b1u) << 10u;
        curr &= ~(0b1u << 9u);
        curr |= (std::to_underlying(REFOUT) & 0b1u) << 9u;
        curr &= ~(0b1u << 8u);
        curr |= (std::to_underlying(REFIN) & 0b1u) << 8u;
        curr &= ~(0b1u << 5u);
        curr |= (std::to_underlying(ALR) & 0b1u) << 5u;
        curr &= ~(0b1u << 4u);
        curr |= (std::to_underlying(CCE) & 0b1u) << 4u;
        curr &= ~(0b1u << 3u);
        curr |= (std::to_underlying(BEI) & 0b1u) << 3u;
        curr &= ~(0b1u << 2u);
        curr |= (std::to_underlying(LEI) & 0b1u) << 2u;
        curr &= ~(0b1u << 1u);
        curr |= (std::to_underlying(CEI) & 0b1u) << 1u;
        curr &= ~(0b1u << 0u);
        curr |= (std::to_underlying(CMI) & 0b1u) << 0u;

        CFG = curr;
    }

    /**
     * Get STS's BEF bit.
     */
    inline bool get_STS_BEF()
    {
        return STS & (1u << 3u);
    }

    /**
     * Set STS's BEF bit.
     */
    inline void set_STS_BEF()
    {
        STS |= 1u << 3u;
    }

    /**
     * Clear STS's BEF bit.
     */
    inline void clear_STS_BEF()
    {
        STS &= ~(1u << 3u);
    }

    /**
     * Toggle STS's BEF bit.
     */
    inline void toggle_STS_BEF()
    {
        STS ^= 1u << 3u;
    }

    /**
     * Get STS's LEF bit.
     */
    inline bool get_STS_LEF()
    {
        return STS & (1u << 2u);
    }

    /**
     * Set STS's LEF bit.
     */
    inline void set_STS_LEF()
    {
        STS |= 1u << 2u;
    }

    /**
     * Clear STS's LEF bit.
     */
    inline void clear_STS_LEF()
    {
        STS &= ~(1u << 2u);
    }

    /**
     * Toggle STS's LEF bit.
     */
    inline void toggle_STS_LEF()
    {
        STS ^= 1u << 2u;
    }

    /**
     * Get STS's CEF bit.
     */
    inline bool get_STS_CEF()
    {
        return STS & (1u << 1u);
    }

    /**
     * Set STS's CEF bit.
     */
    inline void set_STS_CEF()
    {
        STS |= 1u << 1u;
    }

    /**
     * Clear STS's CEF bit.
     */
    inline void clear_STS_CEF()
    {
        STS &= ~(1u << 1u);
    }

    /**
     * Toggle STS's CEF bit.
     */
    inline void toggle_STS_CEF()
    {
        STS ^= 1u << 1u;
    }

    /**
     * Get STS's CMF bit.
     */
    inline bool get_STS_CMF()
    {
        return STS & (1u << 0u);
    }

    /**
     * Set STS's CMF bit.
     */
    inline void set_STS_CMF()
    {
        STS |= 1u << 0u;
    }

    /**
     * Clear STS's CMF bit.
     */
    inline void clear_STS_CMF()
    {
        STS &= ~(1u << 0u);
    }

    /**
     * Toggle STS's CMF bit.
     */
    inline void toggle_STS_CMF()
    {
        STS ^= 1u << 0u;
    }

    /**
     * Get all of STS's bit fields.
     */
    inline void get_STS(bool &BEF, bool &LEF, bool &CEF, bool &CMF)
    {
        uint32_t curr = STS;

        BEF = curr & (1u << 3u);
        LEF = curr & (1u << 2u);
        CEF = curr & (1u << 1u);
        CMF = curr & (1u << 0u);
    }

    /**
     * Set all of STS's bit fields.
     */
    inline void set_STS(bool BEF, bool LEF, bool CEF, bool CMF)
    {
        uint32_t curr = STS;

        curr &= ~(0b1u << 3u);
        curr |= (BEF & 0b1u) << 3u;
        curr &= ~(0b1u << 2u);
        curr |= (LEF & 0b1u) << 2u;
        curr &= ~(0b1u << 1u);
        curr |= (CEF & 0b1u) << 1u;
        curr &= ~(0b1u << 0u);
        curr |= (CMF & 0b1u) << 0u;

        STS = curr;
    }

    /**
     * Get LENGTH's LENGTH field.
     */
    inline uint16_t get_LENGTH_LENGTH()
    {
        return (LENGTH >> 0u) & 0b1111111111111111u;
    }

    /**
     * Set LENGTH's LENGTH field.
     */
    inline void set_LENGTH_LENGTH(uint16_t value)
    {
        uint32_t curr = LENGTH;

        curr &= ~(0b1111111111111111u << 0u);
        curr |= (value & 0b1111111111111111u) << 0u;

        LENGTH = curr;
    }

    /**
     * Get CHECK's CHECK field.
     */
    inline uint32_t get_CHECK_CHECK()
    {
        return (CHECK >> 0u) & 0b11111111111111111111111111111111u;
    }

    /**
     * Set CHECK's CHECK field.
     */
    inline void set_CHECK_CHECK(uint32_t value)
    {
        uint32_t curr = CHECK;

        curr &= ~(0b11111111111111111111111111111111u << 0u);
        curr |= (value & 0b11111111111111111111111111111111u) << 0u;

        CHECK = curr;
    }

    /**
     * Get CRC's CRC field.
     */
    inline uint32_t get_CRC_CRC()
    {
        return (CRC >> 0u) & 0b11111111111111111111111111111111u;
    }

    /**
     * Set CRC's CRC field.
     */
    inline void set_CRC_CRC(uint32_t value)
    {
        uint32_t curr = CRC;

        curr &= ~(0b11111111111111111111111111111111u << 0u);
        curr |= (value & 0b11111111111111111111111111111111u) << 0u;

        CRC = curr;
    }

    /**
     * Get CTR's FRM_CHECK bit.
     */
    inline bool get_CTR_FRM_CHECK()
    {
        return CTR & (1u << 2u);
    }

    /**
     * Set CTR's FRM_CHECK bit.
     */
    inline void set_CTR_FRM_CHECK()
    {
        CTR |= 1u << 2u;
    }

    /**
     * Clear CTR's FRM_CHECK bit.
     */
    inline void clear_CTR_FRM_CHECK()
    {
        CTR &= ~(1u << 2u);
    }

    /**
     * Toggle CTR's FRM_CHECK bit.
     */
    inline void toggle_CTR_FRM_CHECK()
    {
        CTR ^= 1u << 2u;
    }

    /**
     * Get CTR's FRM_CFG bit.
     */
    inline bool get_CTR_FRM_CFG()
    {
        return CTR & (1u << 1u);
    }

    /**
     * Set CTR's FRM_CFG bit.
     */
    inline void set_CTR_FRM_CFG()
    {
        CTR |= 1u << 1u;
    }

    /**
     * Clear CTR's FRM_CFG bit.
     */
    inline void clear_CTR_FRM_CFG()
    {
        CTR &= ~(1u << 1u);
    }

    /**
     * Toggle CTR's FRM_CFG bit.
     */
    inline void toggle_CTR_FRM_CFG()
    {
        CTR ^= 1u << 1u;
    }

    /**
     * Get CTR's FCM bit.
     */
    inline bool get_CTR_FCM()
    {
        return CTR & (1u << 0u);
    }

    /**
     * Set CTR's FCM bit.
     */
    inline void set_CTR_FCM()
    {
        CTR |= 1u << 0u;
    }

    /**
     * Clear CTR's FCM bit.
     */
    inline void clear_CTR_FCM()
    {
        CTR &= ~(1u << 0u);
    }

    /**
     * Toggle CTR's FCM bit.
     */
    inline void toggle_CTR_FCM()
    {
        CTR ^= 1u << 0u;
    }

    /**
     * Get all of CTR's bit fields.
     */
    inline void get_CTR(bool &FRM_CHECK, bool &FRM_CFG, bool &FCM)
    {
        uint32_t curr = CTR;

        FRM_CHECK = curr & (1u << 2u);
        FRM_CFG = curr & (1u << 1u);
        FCM = curr & (1u << 0u);
    }

    /**
     * Set all of CTR's bit fields.
     */
    inline void set_CTR(bool FRM_CHECK, bool FRM_CFG, bool FCM)
    {
        uint32_t curr = CTR;

        curr &= ~(0b1u << 2u);
        curr |= (FRM_CHECK & 0b1u) << 2u;
        curr &= ~(0b1u << 1u);
        curr |= (FRM_CFG & 0b1u) << 1u;
        curr &= ~(0b1u << 0u);
        curr |= (FCM & 0b1u) << 0u;

        CTR = curr;
    }
};

static_assert(sizeof(fce_ke0) == fce_ke0::size);

static volatile fce_ke0 *const FCE_KE0 = reinterpret_cast<fce_ke0 *>(0x50020020);

static volatile fce_ke0 *const FCE_KE1 = reinterpret_cast<fce_ke0 *>(0x50020040);

static volatile fce_ke0 *const FCE_KE2 = reinterpret_cast<fce_ke0 *>(0x50020060);

static volatile fce_ke0 *const FCE_KE3 = reinterpret_cast<fce_ke0 *>(0x50020080);

}; // namespace XMC4700
