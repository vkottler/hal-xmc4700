/**
 * \file
 * \brief Generated by ifgen (3.1.2).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class POSIF0_PCONF_DSEL : uint8_t
{
    a /*!< POSIFx.HSDA */,
    b = 1 /*!< POSIFx.HSDB */
};
static_assert(sizeof(POSIF0_PCONF_DSEL) == 1);

/**
 * Converts POSIF0_PCONF_DSEL to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(POSIF0_PCONF_DSEL instance)
{
    const char *result = "UNKNOWN POSIF0_PCONF_DSEL";

    switch (instance)
    {
    case POSIF0_PCONF_DSEL::a:
        result = "a";
        break;
    case POSIF0_PCONF_DSEL::b:
        result = "b";
        break;
    }

    return result;
}

/**
 * Converts a C string to POSIF0_PCONF_DSEL.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, POSIF0_PCONF_DSEL &output)
{
    bool result = false;

    if ((result = !strncmp(data, "a", 1)))
    {
        output = POSIF0_PCONF_DSEL::a;
    }
    else if ((result = !strncmp(data, "b", 1)))
    {
        output = POSIF0_PCONF_DSEL::b;
    }

    return result;
}

}; // namespace XMC4700
