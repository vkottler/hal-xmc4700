/**
 * \file
 * \brief Generated by ifgen (3.1.2).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class VADC_GLOBRES_FCR : uint8_t
{
    below_compare_value /*!< Signal level was below compare value */,
    above_compare_value = 1 /*!< Signal level was above compare value */
};
static_assert(sizeof(VADC_GLOBRES_FCR) == 1);

/**
 * Converts VADC_GLOBRES_FCR to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(VADC_GLOBRES_FCR instance)
{
    const char *result = "UNKNOWN VADC_GLOBRES_FCR";

    switch (instance)
    {
    case VADC_GLOBRES_FCR::below_compare_value:
        result = "below_compare_value";
        break;
    case VADC_GLOBRES_FCR::above_compare_value:
        result = "above_compare_value";
        break;
    }

    return result;
}

/**
 * Converts a C string to VADC_GLOBRES_FCR.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, VADC_GLOBRES_FCR &output)
{
    bool result = false;

    if ((result = !strncmp(data, "below_compare_value", 19)))
    {
        output = VADC_GLOBRES_FCR::below_compare_value;
    }
    else if ((result = !strncmp(data, "above_compare_value", 19)))
    {
        output = VADC_GLOBRES_FCR::above_compare_value;
    }

    return result;
}

}; // namespace XMC4700
