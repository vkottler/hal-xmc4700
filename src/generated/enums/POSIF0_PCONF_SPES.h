/**
 * \file
 * \brief Generated by ifgen (3.1.2).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class POSIF0_PCONF_SPES : uint8_t
{
    rising_edge /*!< Rising edge */,
    falling_edge = 1 /*!< Falling edge */
};
static_assert(sizeof(POSIF0_PCONF_SPES) == 1);

/**
 * Converts POSIF0_PCONF_SPES to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(POSIF0_PCONF_SPES instance)
{
    const char *result = "UNKNOWN POSIF0_PCONF_SPES";

    switch (instance)
    {
    case POSIF0_PCONF_SPES::rising_edge:
        result = "rising_edge";
        break;
    case POSIF0_PCONF_SPES::falling_edge:
        result = "falling_edge";
        break;
    }

    return result;
}

/**
 * Converts a C string to POSIF0_PCONF_SPES.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, POSIF0_PCONF_SPES &output)
{
    bool result = false;

    if ((result = !strncmp(data, "rising_edge", 11)))
    {
        output = POSIF0_PCONF_SPES::rising_edge;
    }
    else if ((result = !strncmp(data, "falling_edge", 12)))
    {
        output = POSIF0_PCONF_SPES::falling_edge;
    }

    return result;
}

}; // namespace XMC4700
