/**
 * \file
 * \brief Generated by ifgen (3.1.2).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class SDMMC_HOST_CTRL_DATA_TX_WIDTH : uint8_t
{
    1_bit_mode /*!< 1 bit mode */,
    4_bit_mode = 1 /*!< 4-bit mode */
};
static_assert(sizeof(SDMMC_HOST_CTRL_DATA_TX_WIDTH) == 1);

/**
 * Converts SDMMC_HOST_CTRL_DATA_TX_WIDTH to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(SDMMC_HOST_CTRL_DATA_TX_WIDTH instance)
{
    const char *result = "UNKNOWN SDMMC_HOST_CTRL_DATA_TX_WIDTH";

    switch (instance)
    {
    case SDMMC_HOST_CTRL_DATA_TX_WIDTH::1_bit_mode:
        result = "1_bit_mode";
        break;
    case SDMMC_HOST_CTRL_DATA_TX_WIDTH::4_bit_mode:
        result = "4_bit_mode";
        break;
    }

    return result;
}

/**
 * Converts a C string to SDMMC_HOST_CTRL_DATA_TX_WIDTH.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data,
                        SDMMC_HOST_CTRL_DATA_TX_WIDTH &output)
{
    bool result = false;

    if ((result = !strncmp(data, "1_bit_mode", 10)))
    {
        output = SDMMC_HOST_CTRL_DATA_TX_WIDTH::1_bit_mode;
    }
    else if ((result = !strncmp(data, "4_bit_mode", 10)))
    {
        output = SDMMC_HOST_CTRL_DATA_TX_WIDTH::4_bit_mode;
    }

    return result;
}

}; // namespace XMC4700
