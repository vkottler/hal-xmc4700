/**
 * \file
 * \brief Generated by ifgen (3.1.4).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class FLASH0_FCON_SLEEP : uint8_t
{
    normal_state_or_wake_up /*!< Normal state or wake-up */,
    flash_sleep_mode_requested = 1 /*!< Flash sleep mode is requested */
};
static_assert(sizeof(FLASH0_FCON_SLEEP) == 1);

/**
 * Converts FLASH0_FCON_SLEEP to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(FLASH0_FCON_SLEEP instance)
{
    const char *result = "UNKNOWN FLASH0_FCON_SLEEP";

    switch (instance)
    {
    case FLASH0_FCON_SLEEP::normal_state_or_wake_up:
        result = "normal_state_or_wake_up";
        break;
    case FLASH0_FCON_SLEEP::flash_sleep_mode_requested:
        result = "flash_sleep_mode_requested";
        break;
    }

    return result;
}

/**
 * Converts a C string to FLASH0_FCON_SLEEP.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, FLASH0_FCON_SLEEP &output)
{
    bool result = false;

    if ((result = !strncmp(data, "normal_state_or_wake_up", 23)))
    {
        output = FLASH0_FCON_SLEEP::normal_state_or_wake_up;
    }
    else if ((result = !strncmp(data, "flash_sleep_mode_requested", 26)))
    {
        output = FLASH0_FCON_SLEEP::flash_sleep_mode_requested;
    }

    return result;
}

}; // namespace XMC4700
