/**
 * \file
 * \brief Generated by ifgen (2.6.2).
 * {
 *   "enum": {
 *     "value1": {
 *       "description": "Stop channel x",
 *       "value": 0
 *     },
 *     "value2": {
 *       "description": "Demodulator channel x is enabled and runs",
 *       "value": 1
 *     }
 *   },
 *   "unit_test": false,
 *   "json": false,
 *   "use_map": false,
 *   "underlying": "uint8_t",
 *   "namespace": [],
 *   "json_indent": null
 * }
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class DSD_GLOBRC_CH0RUN : uint8_t
{
    value1 /*!< Stop channel x */,
    value2 = 1 /*!< Demodulator channel x is enabled and runs */
};
static_assert(sizeof(DSD_GLOBRC_CH0RUN) == 1);

static constexpr uint16_t DSD_GLOBRC_CH0RUN_id = 2359;

/**
 * Converts DSD_GLOBRC_CH0RUN to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(DSD_GLOBRC_CH0RUN instance)
{
    const char *result = "UNKNOWN DSD_GLOBRC_CH0RUN";

    switch (instance)
    {
    case DSD_GLOBRC_CH0RUN::value1:
        result = "value1";
        break;
    case DSD_GLOBRC_CH0RUN::value2:
        result = "value2";
        break;
    }

    return result;
}

/**
 * Converts a C string to DSD_GLOBRC_CH0RUN.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, DSD_GLOBRC_CH0RUN &output)
{
    bool result = false;

    if ((result = !strncmp(data, "value1", 6)))
    {
        output = DSD_GLOBRC_CH0RUN::value1;
    }
    else if ((result = !strncmp(data, "value2", 6)))
    {
        output = DSD_GLOBRC_CH0RUN::value2;
    }

    return result;
}

}; // namespace XMC4700
