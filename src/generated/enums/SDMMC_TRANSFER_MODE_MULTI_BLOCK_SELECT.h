/**
 * \file
 * \brief Generated by ifgen (3.1.7).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT : uint8_t
{
    single_block /*!< Single Block */,
    multiple_block = 1 /*!< Multiple Block */
};
static_assert(sizeof(SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT) == 1);

/**
 * Converts SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT instance)
{
    const char *result = "UNKNOWN SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT";

    switch (instance)
    {
    case SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT::single_block:
        result = "single_block";
        break;
    case SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT::multiple_block:
        result = "multiple_block";
        break;
    }

    return result;
}

/**
 * Converts a C string to SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data,
                        SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT &output)
{
    bool result = false;

    if ((result = !strncmp(data, "single_block", 12)))
    {
        output = SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT::single_block;
    }
    else if ((result = !strncmp(data, "multiple_block", 14)))
    {
        output = SDMMC_TRANSFER_MODE_MULTI_BLOCK_SELECT::multiple_block;
    }

    return result;
}

}; // namespace XMC4700
