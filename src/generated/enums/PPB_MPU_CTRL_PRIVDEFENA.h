/**
 * \file
 * \brief Generated by ifgen (3.1.1).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class PPB_MPU_CTRL_PRIVDEFENA : uint8_t
{
    value1 /*!< If the MPU is enabled, disables use of the default memory map. Any memory access to a location not covered by any enabled region causes a fault. */,
    value2 = 1 /*!< If the MPU is enabled, enables use of the default memory map as a background region for privileged software accesses. */
};
static_assert(sizeof(PPB_MPU_CTRL_PRIVDEFENA) == 1);

/**
 * Converts PPB_MPU_CTRL_PRIVDEFENA to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(PPB_MPU_CTRL_PRIVDEFENA instance)
{
    const char *result = "UNKNOWN PPB_MPU_CTRL_PRIVDEFENA";

    switch (instance)
    {
    case PPB_MPU_CTRL_PRIVDEFENA::value1:
        result = "value1";
        break;
    case PPB_MPU_CTRL_PRIVDEFENA::value2:
        result = "value2";
        break;
    }

    return result;
}

/**
 * Converts a C string to PPB_MPU_CTRL_PRIVDEFENA.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, PPB_MPU_CTRL_PRIVDEFENA &output)
{
    bool result = false;

    if ((result = !strncmp(data, "value1", 6)))
    {
        output = PPB_MPU_CTRL_PRIVDEFENA::value1;
    }
    else if ((result = !strncmp(data, "value2", 6)))
    {
        output = PPB_MPU_CTRL_PRIVDEFENA::value2;
    }

    return result;
}

}; // namespace XMC4700
