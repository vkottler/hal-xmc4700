/**
 * \file
 * \brief Generated by ifgen (3.1.6).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class DSD_OCS_SUSSTA : uint8_t
{
    not_suspended /*!< Module is not (yet) suspended */,
    suspended = 1 /*!< Module is suspended */
};
static_assert(sizeof(DSD_OCS_SUSSTA) == 1);

/**
 * Converts DSD_OCS_SUSSTA to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(DSD_OCS_SUSSTA instance)
{
    const char *result = "UNKNOWN DSD_OCS_SUSSTA";

    switch (instance)
    {
    case DSD_OCS_SUSSTA::not_suspended:
        result = "not_suspended";
        break;
    case DSD_OCS_SUSSTA::suspended:
        result = "suspended";
        break;
    }

    return result;
}

/**
 * Converts a C string to DSD_OCS_SUSSTA.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, DSD_OCS_SUSSTA &output)
{
    bool result = false;

    if ((result = !strncmp(data, "not_suspended", 13)))
    {
        output = DSD_OCS_SUSSTA::not_suspended;
    }
    else if ((result = !strncmp(data, "suspended", 9)))
    {
        output = DSD_OCS_SUSSTA::suspended;
    }

    return result;
}

}; // namespace XMC4700
