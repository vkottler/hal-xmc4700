/**
 * \file
 * \brief Generated by ifgen (3.0.0).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class PPB_AIRCR_ENDIANNESS : uint8_t
{
    value1 /*!< Little-endian */,
    value2 = 1 /*!< Big-endian. */
};
static_assert(sizeof(PPB_AIRCR_ENDIANNESS) == 1);

static constexpr uint16_t PPB_AIRCR_ENDIANNESS_id = 777;

/**
 * Converts PPB_AIRCR_ENDIANNESS to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(PPB_AIRCR_ENDIANNESS instance)
{
    const char *result = "UNKNOWN PPB_AIRCR_ENDIANNESS";

    switch (instance)
    {
    case PPB_AIRCR_ENDIANNESS::value1:
        result = "value1";
        break;
    case PPB_AIRCR_ENDIANNESS::value2:
        result = "value2";
        break;
    }

    return result;
}

/**
 * Converts a C string to PPB_AIRCR_ENDIANNESS.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, PPB_AIRCR_ENDIANNESS &output)
{
    bool result = false;

    if ((result = !strncmp(data, "value1", 6)))
    {
        output = PPB_AIRCR_ENDIANNESS::value1;
    }
    else if ((result = !strncmp(data, "value2", 6)))
    {
        output = PPB_AIRCR_ENDIANNESS::value2;
    }

    return result;
}

}; // namespace XMC4700
