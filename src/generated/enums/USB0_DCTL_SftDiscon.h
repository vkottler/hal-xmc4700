/**
 * \file
 * \brief Generated by ifgen (3.1.7).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class USB0_DCTL_SftDiscon : uint8_t
{
    normal_operation_when_this_bit_cleared_after__cont /*!< Normal operation.
                                                          When this bit is
                                                          cleared after a soft
                                                          disconnect, the core
                                                          drives a device
                                                          connect event to the
                                                          USB host. When the
                                                          device is
                                                          reconnected, the USB
                                                          host restarts device
                                                          enumeration. */
        ,
    core_drives_device_disconnect_event_usb_host =
        1 /*!< The core drives a device disconnect event to the USB host. */
};
static_assert(sizeof(USB0_DCTL_SftDiscon) == 1);

/**
 * Converts USB0_DCTL_SftDiscon to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(USB0_DCTL_SftDiscon instance)
{
    const char *result = "UNKNOWN USB0_DCTL_SftDiscon";

    switch (instance)
    {
    case USB0_DCTL_SftDiscon::
        normal_operation_when_this_bit_cleared_after__cont:
        result = "normal_operation_when_this_bit_cleared_after__cont";
        break;
    case USB0_DCTL_SftDiscon::core_drives_device_disconnect_event_usb_host:
        result = "core_drives_device_disconnect_event_usb_host";
        break;
    }

    return result;
}

/**
 * Converts a C string to USB0_DCTL_SftDiscon.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, USB0_DCTL_SftDiscon &output)
{
    bool result = false;

    if ((result = !strncmp(
             data, "normal_operation_when_this_bit_cleared_after__cont", 50)))
    {
        output = USB0_DCTL_SftDiscon::
            normal_operation_when_this_bit_cleared_after__cont;
    }
    else if ((result = !strncmp(
                  data, "core_drives_device_disconnect_event_usb_host", 44)))
    {
        output =
            USB0_DCTL_SftDiscon::core_drives_device_disconnect_event_usb_host;
    }

    return result;
}

}; // namespace XMC4700
