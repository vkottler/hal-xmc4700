/**
 * \file
 * \brief Generated by ifgen (2.6.2).
 * {
 *   "enum": {
 *     "value1": {
 *       "description": "No refresh error.",
 *       "value": 0
 *     },
 *     "value2": {
 *       "description": "Refresh error occurred.",
 *       "value": 1
 *     }
 *   },
 *   "unit_test": false,
 *   "json": false,
 *   "use_map": false,
 *   "underlying": "uint8_t",
 *   "namespace": [],
 *   "json_indent": null
 * }
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class EBU_SDRSTAT_REFERR : uint8_t
{
    value1 /*!< No refresh error. */,
    value2 = 1 /*!< Refresh error occurred. */
};
static_assert(sizeof(EBU_SDRSTAT_REFERR) == 1);

static constexpr uint16_t EBU_SDRSTAT_REFERR_id = 553;

/**
 * Converts EBU_SDRSTAT_REFERR to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(EBU_SDRSTAT_REFERR instance)
{
    const char *result = "UNKNOWN EBU_SDRSTAT_REFERR";

    switch (instance)
    {
    case EBU_SDRSTAT_REFERR::value1:
        result = "value1";
        break;
    case EBU_SDRSTAT_REFERR::value2:
        result = "value2";
        break;
    }

    return result;
}

/**
 * Converts a C string to EBU_SDRSTAT_REFERR.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, EBU_SDRSTAT_REFERR &output)
{
    bool result = false;

    if ((result = !strncmp(data, "value1", 6)))
    {
        output = EBU_SDRSTAT_REFERR::value1;
    }
    else if ((result = !strncmp(data, "value2", 6)))
    {
        output = EBU_SDRSTAT_REFERR::value2;
    }

    return result;
}

}; // namespace XMC4700
