/**
 * \file
 * \brief Generated by ifgen (3.1.2).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class EBU_SDRSTAT_REFERR : uint8_t
{
    no_refresh_error /*!< No refresh error. */,
    refresh_error_occurred = 1 /*!< Refresh error occurred. */
};
static_assert(sizeof(EBU_SDRSTAT_REFERR) == 1);

/**
 * Converts EBU_SDRSTAT_REFERR to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(EBU_SDRSTAT_REFERR instance)
{
    const char *result = "UNKNOWN EBU_SDRSTAT_REFERR";

    switch (instance)
    {
    case EBU_SDRSTAT_REFERR::no_refresh_error:
        result = "no_refresh_error";
        break;
    case EBU_SDRSTAT_REFERR::refresh_error_occurred:
        result = "refresh_error_occurred";
        break;
    }

    return result;
}

/**
 * Converts a C string to EBU_SDRSTAT_REFERR.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, EBU_SDRSTAT_REFERR &output)
{
    bool result = false;

    if ((result = !strncmp(data, "no_refresh_error", 16)))
    {
        output = EBU_SDRSTAT_REFERR::no_refresh_error;
    }
    else if ((result = !strncmp(data, "refresh_error_occurred", 22)))
    {
        output = EBU_SDRSTAT_REFERR::refresh_error_occurred;
    }

    return result;
}

}; // namespace XMC4700
