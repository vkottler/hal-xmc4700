/**
 * \file
 * \brief Generated by ifgen (3.1.7).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class PPB_HFSR_FORCED : uint8_t
{
    no_forced_hardfault /*!< no forced HardFault */,
    forced_hardfault = 1 /*!< forced HardFault. */
};
static_assert(sizeof(PPB_HFSR_FORCED) == 1);

/**
 * Converts PPB_HFSR_FORCED to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(PPB_HFSR_FORCED instance)
{
    const char *result = "UNKNOWN PPB_HFSR_FORCED";

    switch (instance)
    {
    case PPB_HFSR_FORCED::no_forced_hardfault:
        result = "no_forced_hardfault";
        break;
    case PPB_HFSR_FORCED::forced_hardfault:
        result = "forced_hardfault";
        break;
    }

    return result;
}

/**
 * Converts a C string to PPB_HFSR_FORCED.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, PPB_HFSR_FORCED &output)
{
    bool result = false;

    if ((result = !strncmp(data, "no_forced_hardfault", 19)))
    {
        output = PPB_HFSR_FORCED::no_forced_hardfault;
    }
    else if ((result = !strncmp(data, "forced_hardfault", 16)))
    {
        output = PPB_HFSR_FORCED::forced_hardfault;
    }

    return result;
}

}; // namespace XMC4700
