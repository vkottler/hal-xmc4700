/**
 * \file
 * \brief Generated by ifgen (3.1.2).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class SDMMC_PRESENT_STATE_CARD_STATE_STABLE : uint8_t
{
    reset_debouncing /*!< Reset of Debouncing */,
    no_card_or_inserted = 1 /*!< No Card or Inserted */
};
static_assert(sizeof(SDMMC_PRESENT_STATE_CARD_STATE_STABLE) == 1);

/**
 * Converts SDMMC_PRESENT_STATE_CARD_STATE_STABLE to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(SDMMC_PRESENT_STATE_CARD_STATE_STABLE instance)
{
    const char *result = "UNKNOWN SDMMC_PRESENT_STATE_CARD_STATE_STABLE";

    switch (instance)
    {
    case SDMMC_PRESENT_STATE_CARD_STATE_STABLE::reset_debouncing:
        result = "reset_debouncing";
        break;
    case SDMMC_PRESENT_STATE_CARD_STATE_STABLE::no_card_or_inserted:
        result = "no_card_or_inserted";
        break;
    }

    return result;
}

/**
 * Converts a C string to SDMMC_PRESENT_STATE_CARD_STATE_STABLE.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data,
                        SDMMC_PRESENT_STATE_CARD_STATE_STABLE &output)
{
    bool result = false;

    if ((result = !strncmp(data, "reset_debouncing", 16)))
    {
        output = SDMMC_PRESENT_STATE_CARD_STATE_STABLE::reset_debouncing;
    }
    else if ((result = !strncmp(data, "no_card_or_inserted", 19)))
    {
        output = SDMMC_PRESENT_STATE_CARD_STATE_STABLE::no_card_or_inserted;
    }

    return result;
}

}; // namespace XMC4700
