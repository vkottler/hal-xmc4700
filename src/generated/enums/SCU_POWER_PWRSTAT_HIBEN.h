/**
 * \file
 * \brief Generated by ifgen (3.1.4).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class SCU_POWER_PWRSTAT_HIBEN : uint8_t
{
    inactive /*!< Inactive */,
    active = 1 /*!< Active */
};
static_assert(sizeof(SCU_POWER_PWRSTAT_HIBEN) == 1);

/**
 * Converts SCU_POWER_PWRSTAT_HIBEN to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(SCU_POWER_PWRSTAT_HIBEN instance)
{
    const char *result = "UNKNOWN SCU_POWER_PWRSTAT_HIBEN";

    switch (instance)
    {
    case SCU_POWER_PWRSTAT_HIBEN::inactive:
        result = "inactive";
        break;
    case SCU_POWER_PWRSTAT_HIBEN::active:
        result = "active";
        break;
    }

    return result;
}

/**
 * Converts a C string to SCU_POWER_PWRSTAT_HIBEN.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, SCU_POWER_PWRSTAT_HIBEN &output)
{
    bool result = false;

    if ((result = !strncmp(data, "inactive", 8)))
    {
        output = SCU_POWER_PWRSTAT_HIBEN::inactive;
    }
    else if ((result = !strncmp(data, "active", 6)))
    {
        output = SCU_POWER_PWRSTAT_HIBEN::active;
    }

    return result;
}

}; // namespace XMC4700
