/**
 * \file
 * \brief Generated by ifgen (3.1.7).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class SCU_PLL_PLLCON0_VCOPWD : uint8_t
{
    normal_behavior /*!< Normal behavior */,
    vco_put_into_power_saving_mode_can_no_longer__cont =
        1 /*!< The VCO is put into a Power Saving Mode and can no longer be
             used. Only the Bypass and Prescaler Mode are active if previously
             selected. */
};
static_assert(sizeof(SCU_PLL_PLLCON0_VCOPWD) == 1);

/**
 * Converts SCU_PLL_PLLCON0_VCOPWD to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(SCU_PLL_PLLCON0_VCOPWD instance)
{
    const char *result = "UNKNOWN SCU_PLL_PLLCON0_VCOPWD";

    switch (instance)
    {
    case SCU_PLL_PLLCON0_VCOPWD::normal_behavior:
        result = "normal_behavior";
        break;
    case SCU_PLL_PLLCON0_VCOPWD::
        vco_put_into_power_saving_mode_can_no_longer__cont:
        result = "vco_put_into_power_saving_mode_can_no_longer__cont";
        break;
    }

    return result;
}

/**
 * Converts a C string to SCU_PLL_PLLCON0_VCOPWD.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, SCU_PLL_PLLCON0_VCOPWD &output)
{
    bool result = false;

    if ((result = !strncmp(data, "normal_behavior", 15)))
    {
        output = SCU_PLL_PLLCON0_VCOPWD::normal_behavior;
    }
    else if ((result = !strncmp(
                  data, "vco_put_into_power_saving_mode_can_no_longer__cont",
                  50)))
    {
        output = SCU_PLL_PLLCON0_VCOPWD::
            vco_put_into_power_saving_mode_can_no_longer__cont;
    }

    return result;
}

}; // namespace XMC4700
