/**
 * \file
 * \brief Generated by ifgen (3.1.7).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL : uint8_t
{
    no_card /*!< No Card */,
    card_inserted = 1 /*!< Card Inserted */
};
static_assert(sizeof(SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL) == 1);

/**
 * Converts SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL instance)
{
    const char *result = "UNKNOWN SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL";

    switch (instance)
    {
    case SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL::no_card:
        result = "no_card";
        break;
    case SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL::card_inserted:
        result = "card_inserted";
        break;
    }

    return result;
}

/**
 * Converts a C string to SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data,
                        SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL &output)
{
    bool result = false;

    if ((result = !strncmp(data, "no_card", 7)))
    {
        output = SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL::no_card;
    }
    else if ((result = !strncmp(data, "card_inserted", 13)))
    {
        output = SDMMC_HOST_CTRL_CARD_DETECT_TEST_LEVEL::card_inserted;
    }

    return result;
}

}; // namespace XMC4700
