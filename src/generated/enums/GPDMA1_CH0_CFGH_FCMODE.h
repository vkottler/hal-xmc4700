/**
 * \file
 * \brief Generated by ifgen (3.1.2).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class GPDMA1_CH0_CFGH_FCMODE : uint8_t
{
    serviced_when_the_cont /*!< Source transaction requests are serviced when
                              they occur. Data pre-fetching is enabled. */
        ,
    not_serviced_unti_cont =
        1 /*!< Source transaction requests are not serviced until a destination
             transaction request occurs. In this mode, the amount of data
             transferred from the source is limited so that it is guaranteed to
             be transferred to the destination prior to block termination by
             the destination. Data pre-fetching is disabled. */
};
static_assert(sizeof(GPDMA1_CH0_CFGH_FCMODE) == 1);

/**
 * Converts GPDMA1_CH0_CFGH_FCMODE to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(GPDMA1_CH0_CFGH_FCMODE instance)
{
    const char *result = "UNKNOWN GPDMA1_CH0_CFGH_FCMODE";

    switch (instance)
    {
    case GPDMA1_CH0_CFGH_FCMODE::serviced_when_the_cont:
        result = "serviced_when_the_cont";
        break;
    case GPDMA1_CH0_CFGH_FCMODE::not_serviced_unti_cont:
        result = "not_serviced_unti_cont";
        break;
    }

    return result;
}

/**
 * Converts a C string to GPDMA1_CH0_CFGH_FCMODE.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, GPDMA1_CH0_CFGH_FCMODE &output)
{
    bool result = false;

    if ((result = !strncmp(data, "serviced_when_the_cont", 22)))
    {
        output = GPDMA1_CH0_CFGH_FCMODE::serviced_when_the_cont;
    }
    else if ((result = !strncmp(data, "not_serviced_unti_cont", 22)))
    {
        output = GPDMA1_CH0_CFGH_FCMODE::not_serviced_unti_cont;
    }

    return result;
}

}; // namespace XMC4700
