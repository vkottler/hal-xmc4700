/**
 * \file
 * \brief Generated by ifgen (3.1.2).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class POSIF0_PCONF_MCUE : uint8_t
{
    hw /*!< Multi-Channel pattern update is controlled via HW */,
    sw = 1 /*!< Multi-Channel pattern update is controlled via SW */
};
static_assert(sizeof(POSIF0_PCONF_MCUE) == 1);

/**
 * Converts POSIF0_PCONF_MCUE to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(POSIF0_PCONF_MCUE instance)
{
    const char *result = "UNKNOWN POSIF0_PCONF_MCUE";

    switch (instance)
    {
    case POSIF0_PCONF_MCUE::hw:
        result = "hw";
        break;
    case POSIF0_PCONF_MCUE::sw:
        result = "sw";
        break;
    }

    return result;
}

/**
 * Converts a C string to POSIF0_PCONF_MCUE.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, POSIF0_PCONF_MCUE &output)
{
    bool result = false;

    if ((result = !strncmp(data, "hw", 2)))
    {
        output = POSIF0_PCONF_MCUE::hw;
    }
    else if ((result = !strncmp(data, "sw", 2)))
    {
        output = POSIF0_PCONF_MCUE::sw;
    }

    return result;
}

}; // namespace XMC4700
