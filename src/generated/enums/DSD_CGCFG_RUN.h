/**
 * \file
 * \brief Generated by ifgen (3.1.7).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class DSD_CGCFG_RUN : uint8_t
{
    stopped_cleared_at_end_period /*!< Stopped (cleared at the end of a period)
                                   */
        ,
    running = 1 /*!< Running */
};
static_assert(sizeof(DSD_CGCFG_RUN) == 1);

/**
 * Converts DSD_CGCFG_RUN to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(DSD_CGCFG_RUN instance)
{
    const char *result = "UNKNOWN DSD_CGCFG_RUN";

    switch (instance)
    {
    case DSD_CGCFG_RUN::stopped_cleared_at_end_period:
        result = "stopped_cleared_at_end_period";
        break;
    case DSD_CGCFG_RUN::running:
        result = "running";
        break;
    }

    return result;
}

/**
 * Converts a C string to DSD_CGCFG_RUN.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data, DSD_CGCFG_RUN &output)
{
    bool result = false;

    if ((result = !strncmp(data, "stopped_cleared_at_end_period", 29)))
    {
        output = DSD_CGCFG_RUN::stopped_cleared_at_end_period;
    }
    else if ((result = !strncmp(data, "running", 7)))
    {
        output = DSD_CGCFG_RUN::running;
    }

    return result;
}

}; // namespace XMC4700
