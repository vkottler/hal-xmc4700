/**
 * \file
 * \brief Generated by ifgen (3.1.2).
 */
#pragma once

#include <cstdint>
#include <cstring>

namespace XMC4700
{

enum class SDMMC_PRESENT_STATE_CARD_INSERTED : uint8_t
{
    reset_or_debouncing_or_no_card /*!< Reset or Debouncing or No Card */,
    card_inserted = 1 /*!< Card Inserted */
};
static_assert(sizeof(SDMMC_PRESENT_STATE_CARD_INSERTED) == 1);

/**
 * Converts SDMMC_PRESENT_STATE_CARD_INSERTED to a C string.
 *
 * \param[in] instance Value to convert.
 * \return             A C string representation of the value.
 */
inline const char *to_string(SDMMC_PRESENT_STATE_CARD_INSERTED instance)
{
    const char *result = "UNKNOWN SDMMC_PRESENT_STATE_CARD_INSERTED";

    switch (instance)
    {
    case SDMMC_PRESENT_STATE_CARD_INSERTED::reset_or_debouncing_or_no_card:
        result = "reset_or_debouncing_or_no_card";
        break;
    case SDMMC_PRESENT_STATE_CARD_INSERTED::card_inserted:
        result = "card_inserted";
        break;
    }

    return result;
}

/**
 * Converts a C string to SDMMC_PRESENT_STATE_CARD_INSERTED.
 *
 * \param[in]  data   A C string to convert.
 * \param[out] output The enumeration element to write.
 * \return            Whether or not the output was written.
 */
inline bool from_string(const char *data,
                        SDMMC_PRESENT_STATE_CARD_INSERTED &output)
{
    bool result = false;

    if ((result = !strncmp(data, "reset_or_debouncing_or_no_card", 30)))
    {
        output =
            SDMMC_PRESENT_STATE_CARD_INSERTED::reset_or_debouncing_or_no_card;
    }
    else if ((result = !strncmp(data, "card_inserted", 13)))
    {
        output = SDMMC_PRESENT_STATE_CARD_INSERTED::card_inserted;
    }

    return result;
}

}; // namespace XMC4700
